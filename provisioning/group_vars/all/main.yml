---
# =============================================================================
# Shared scenario facts (non-secret) available to every play.
# =============================================================================

# Network layout (mirrors /topology.yml)
soar_net_cidr: "10.10.30.0/24"
attacker_net_cidr: "10.10.10.0/24"
target_net_cidr: "10.10.20.0/24"

# Firewall gateway interface IPs (lab-firewall is on all three nets)
fw_soar_ip: "10.10.30.1"
fw_attacker_ip: "10.10.10.1"
fw_target_ip: "10.10.20.1"

# Key actors
ng_soar_ip: "10.10.30.10"
student_ws_ip: "10.10.30.20"
evaluation_ip: "10.10.30.30"

# The malicious test source IP (attacker) the CACAO playbook must block.
malicious_source_ip: "10.10.10.10"

# The protected service.
target_service_ip: "10.10.20.10"
target_service_http_port: 80
target_service_ssh_port: 22

# Management account NG-SOAR uses to SSH into the lab-firewall to apply rules.
fw_mgmt_user: "soar-fw"
