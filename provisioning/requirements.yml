---
# Ansible collections required by the provisioning roles.
collections:
  - name: community.docker
    version: ">=3.4.0"
  - name: community.crypto
    version: ">=2.10.0"
  - name: ansible.posix
    version: ">=1.5.0"
